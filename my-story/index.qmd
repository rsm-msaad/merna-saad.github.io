---
title: "My Story"
pagetitle: "My Story"
page-layout: full
toc: false
title-block-banner: false
title-block-style: none
format:
  html:
    css: ../assets/my-story.css
    body-classes: my-story-page
    include-in-header:
      - text: |
          <link rel="preconnect" href="https://fonts.googleapis.com">
          <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
          <link href="https://fonts.googleapis.com/css2?family=Caveat:wght@500;700&display=swap" rel="stylesheet">
          <script>
          (function ensureBodyClass(){
            function add(){
              if (document.body) { document.body.classList.add('my-story-page'); }
              else { setTimeout(add, 5); }
            }
            add();
          })();
          </script>
---

```{=html}
<script>
(function loadGSAP() {
  var savedDefine = window.define;
  window.define = undefined;
  var s1 = document.createElement('script');
  s1.src = 'https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.5/gsap.min.js';
  s1.onload = function() {
    window.define = savedDefine;
    document.dispatchEvent(new Event('gsap-ready'));
  };
  s1.onerror = function() {
    window.define = savedDefine;
    document.dispatchEvent(new Event('gsap-ready'));
  };
  document.head.appendChild(s1);
})();
</script>

<link rel="preload" as="video" href="../assets/my-story-beat-00-intro.mp4" type="video/mp4">
<link rel="preload" as="image" href="../assets/about-beat-01-cairo.webp" type="image/webp">
<link rel="preload" as="image" href="../assets/about-beat-02-auc.webp" type="image/webp">

<div class="gs-theater" id="gs-theater">
  <a href="/" class="gs-back-link" aria-label="Back to home">
    <span class="arrow" aria-hidden="true">&larr;</span>
    <span>merna.org</span>
  </a>
  <div class="gs-screen" id="gs-screen">
    <div style="text-align:center;padding:40px;color:#5a5a5a;font-style:italic;">Loading story...</div>
  </div>
  <div class="gs-timeline-bar">
    <div class="gs-timeline-fill" id="gs-tl-fill"></div>
  </div>
  <div class="gs-nav-controls">
    <button class="gs-nav-btn gs-nav-prev" id="gs-prev" disabled>Previous</button>
    <span class="gs-nav-counter" id="gs-counter">1 / 13</span>
    <button class="gs-nav-btn gs-nav-next" id="gs-next">Next</button>
  </div>
  <div class="gs-nav-restart"><button class="gs-restart-btn" id="gs-restart">Restart</button></div>
</div>

<script>
function initAboutTheater() {
  var hasGsap = typeof gsap !== 'undefined';
  var screen = document.getElementById('gs-screen');
  var tlFill = document.getElementById('gs-tl-fill');
  var prevBtn = document.getElementById('gs-prev');
  var nextBtn = document.getElementById('gs-next');
  var counterEl = document.getElementById('gs-counter');
  var restartBtn = document.getElementById('gs-restart');
  var currentScene = 0;

  function clearScreen() {
    if (hasGsap) gsap.killTweensOf(screen.querySelectorAll('*'));
    screen.innerHTML = '';
    screen.style.opacity = 1;
  }

  function makeScene(imgSrc, noteText) {
    var webpSrc = imgSrc.replace('.png', '.webp');
    return function() {
      screen.innerHTML =
        '<div class="gs-about-scene">' +
          '<div class="gs-about-image"><picture>' +
            '<source srcset="' + webpSrc + '" type="image/webp">' +
            '<img src="' + imgSrc + '" alt="" loading="lazy" />' +
          '</picture></div>' +
          '<div class="gs-about-note"><div class="gs-bubble" style="text-align:left !important;"><p>' + noteText + '</p></div></div>' +
        '</div>';
      if (hasGsap) {
        gsap.fromTo(screen.querySelector('.gs-about-image'),
          { x: -20, opacity: 0 }, { x: 0, opacity: 1, duration: 0.4, ease: 'power2.out' });
        gsap.fromTo(screen.querySelector('.gs-about-note'),
          { opacity: 0 }, { opacity: 1, duration: 0.4, ease: 'power2.out', delay: 0.15 });
      }
    };
  }

  var scenes = [
    function() {
      screen.innerHTML =
        '<div class="gs-intro-fullscreen">' +
          '<video class="gs-intro-video" src="../assets/my-story-beat-00-intro.mp4" poster="../assets/about-beat-00-intro.png" autoplay loop muted playsinline preload="auto"></video>' +
          '<div class="gs-intro-vignette"></div>' +
          '<div class="gs-intro-text-wrap">' +
            '<p class="gs-intro-text" id="gs-intro-text"></p>' +
            '<span class="gs-intro-cursor" id="gs-intro-cursor"></span>' +
          '</div>' +
        '</div>';
      var vid = screen.querySelector('video.gs-intro-video');
      if (vid) { var p = vid.play(); if (p && p.catch) p.catch(function(){}); }

      // Typewriter effect for the intro text
      var fullText = "Hi, I'm Merna. Welcome to my story. Click next to walk through how I got here.";
      var textEl = document.getElementById('gs-intro-text');
      var cursorEl = document.getElementById('gs-intro-cursor');
      if (window.__introTypeTimer) { clearTimeout(window.__introTypeTimer); }
      var i = 0;
      function type() {
        var el = document.getElementById('gs-intro-text');
        if (!el) return;
        if (i < fullText.length) {
          el.textContent += fullText.charAt(i);
          i++;
          var ch = fullText.charAt(i - 1);
          var delay = (ch === '.' || ch === ',') ? 220 : 38;
          window.__introTypeTimer = setTimeout(type, delay);
        } else {
          var c = document.getElementById('gs-intro-cursor');
          if (c) c.classList.add('done');
        }
      }
      window.__introTypeTimer = setTimeout(type, 650);

      if (hasGsap) {
        gsap.fromTo(screen.querySelector('.gs-intro-fullscreen'),
          { opacity: 0 }, { opacity: 1, duration: 0.6, ease: 'power2.out' });
        gsap.fromTo(screen.querySelector('.gs-intro-text-wrap'),
          { y: 14, opacity: 0 }, { y: 0, opacity: 1, duration: 0.7, ease: 'power2.out', delay: 0.35 });
      }
    },
    function() {
      var captionText = "My story starts in Cairo. The city I was born in, grew up in, and still carry with me everywhere I go.";
      var captionLetters = '';
      var words = captionText.split(' ');
      for (var w = 0; w < words.length; w++) {
        captionLetters += '<span class="cap-word">';
        for (var c = 0; c < words[w].length; c++) {
          var ch = words[w].charAt(c);
          captionLetters += '<span class="cap-letter">' + ch.replace('<', '&lt;').replace('>', '&gt;') + '</span>';
        }
        captionLetters += '</span>';
        if (w < words.length - 1) {
          captionLetters += '<span class="cap-letter cap-space">&nbsp;</span>';
        }
      }
      screen.innerHTML =
        '<div class="gs-intro-fullscreen">' +
          '<video class="gs-intro-video" src="../assets/my-story-beat-01-cairo.mp4" poster="../assets/about-beat-01-cairo.png" autoplay loop muted playsinline preload="auto"></video>' +
          '<div class="story-beat-caption"><span class="story-beat-caption-inner">' + captionLetters + '</span></div>' +
        '</div>';

      var vid = screen.querySelector('video.gs-intro-video');
      if (vid) { var pp = vid.play(); if (pp && pp.catch) pp.catch(function(){}); }

      var letters = screen.querySelectorAll('.story-beat-caption .cap-letter');
      if (hasGsap && letters.length) {
        gsap.set(letters, { opacity: 0, y: 4 });
        gsap.to(letters, {
          opacity: 1,
          y: 0,
          duration: 0.18,
          ease: 'power1.out',
          stagger: 0.05,
          delay: 0.5
        });
      } else if (letters.length) {
        for (var li = 0; li < letters.length; li++) {
          (function(el, idx){
            setTimeout(function(){ el.style.opacity = 1; }, 500 + idx * 50);
          })(letters[li], li);
        }
      }

      if (hasGsap) {
        gsap.fromTo(screen.querySelector('.gs-intro-fullscreen'),
          { opacity: 0 }, { opacity: 1, duration: 0.6, ease: 'power2.out' });
      }
    },
    function() {
      var captionText = "I graduated from the American University in Cairo in 2014, with dual degrees in Architectural Engineering and Finance. Buildings taught me systems, finance taught me numbers. The combination became my way of seeing the world.";
      var captionLetters = '';
      var words = captionText.split(' ');
      for (var w = 0; w < words.length; w++) {
        captionLetters += '<span class="cap-word">';
        for (var c = 0; c < words[w].length; c++) {
          var ch = words[w].charAt(c);
          captionLetters += '<span class="cap-letter">' + ch.replace('<', '&lt;').replace('>', '&gt;') + '</span>';
        }
        captionLetters += '</span>';
        if (w < words.length - 1) {
          captionLetters += '<span class="cap-letter cap-space">&nbsp;</span>';
        }
      }
      screen.innerHTML =
        '<div class="gs-intro-fullscreen">' +
          '<video class="gs-intro-video" src="../assets/my-story-beat-02-auc.mp4" poster="../assets/about-beat-02-auc.png" autoplay loop muted playsinline preload="auto"></video>' +
          '<div class="story-beat-caption"><span class="story-beat-caption-inner">' + captionLetters + '</span></div>' +
        '</div>';

      var vid = screen.querySelector('video.gs-intro-video');
      if (vid) { var pp = vid.play(); if (pp && pp.catch) pp.catch(function(){}); }

      var letters = screen.querySelectorAll('.story-beat-caption .cap-letter');
      if (hasGsap && letters.length) {
        gsap.set(letters, { opacity: 0, y: 4 });
        gsap.to(letters, {
          opacity: 1,
          y: 0,
          duration: 0.18,
          ease: 'power1.out',
          stagger: 0.05,
          delay: 0.5
        });
      } else if (letters.length) {
        for (var li = 0; li < letters.length; li++) {
          (function(el, idx){
            setTimeout(function(){ el.style.opacity = 1; }, 500 + idx * 50);
          })(letters[li], li);
        }
      }

      if (hasGsap) {
        gsap.fromTo(screen.querySelector('.gs-intro-fullscreen'),
          { opacity: 0 }, { opacity: 1, duration: 0.6, ease: 'power2.out' });
      }
    },
    function() {
      var captionText = "My first job was at Corplease, Egypt's largest leasing company. Four quiet years of learning the craft, building expertise one credit file at a time.";
      var captionLetters = '';
      var words = captionText.split(' ');
      for (var w = 0; w < words.length; w++) {
        captionLetters += '<span class="cap-word">';
        for (var c = 0; c < words[w].length; c++) {
          var ch = words[w].charAt(c);
          captionLetters += '<span class="cap-letter">' + ch.replace('<', '&lt;').replace('>', '&gt;') + '</span>';
        }
        captionLetters += '</span>';
        if (w < words.length - 1) {
          captionLetters += '<span class="cap-letter cap-space">&nbsp;</span>';
        }
      }
      screen.innerHTML =
        '<div class="gs-intro-fullscreen">' +
          '<video class="gs-intro-video" src="../assets/my-story-beat-03-corplease-early.mp4" poster="../assets/about-beat-03-corplease-early.png" autoplay loop muted playsinline preload="auto"></video>' +
          '<div class="story-beat-caption"><span class="story-beat-caption-inner">' + captionLetters + '</span></div>' +
        '</div>';

      var vid = screen.querySelector('video.gs-intro-video');
      if (vid) { var pp = vid.play(); if (pp && pp.catch) pp.catch(function(){}); }

      var letters = screen.querySelectorAll('.story-beat-caption .cap-letter');
      if (hasGsap && letters.length) {
        gsap.set(letters, { opacity: 0, y: 4 });
        gsap.to(letters, {
          opacity: 1,
          y: 0,
          duration: 0.18,
          ease: 'power1.out',
          stagger: 0.05,
          delay: 0.5
        });
      } else if (letters.length) {
        for (var li = 0; li < letters.length; li++) {
          (function(el, idx){
            setTimeout(function(){ el.style.opacity = 1; }, 500 + idx * 50);
          })(letters[li], li);
        }
      }

      if (hasGsap) {
        gsap.fromTo(screen.querySelector('.gs-intro-fullscreen'),
          { opacity: 0 }, { opacity: 1, duration: 0.6, ease: 'power2.out' });
      }
    },
    function() {
      var captionText = "In 2018 I became Manager, leading a team of six analysts. This is where I taught myself Python and built automated pipelines that cut our credit assessment time by forty percent. A finance team running on code was rare in Egypt then. I was hooked.";
      var captionLetters = '';
      var words = captionText.split(' ');
      for (var w = 0; w < words.length; w++) {
        captionLetters += '<span class="cap-word">';
        for (var c = 0; c < words[w].length; c++) {
          var ch = words[w].charAt(c);
          captionLetters += '<span class="cap-letter">' + ch.replace('<', '&lt;').replace('>', '&gt;') + '</span>';
        }
        captionLetters += '</span>';
        if (w < words.length - 1) {
          captionLetters += '<span class="cap-letter cap-space">&nbsp;</span>';
        }
      }
      screen.innerHTML =
        '<div class="gs-intro-fullscreen">' +
          '<video class="gs-intro-video" src="../assets/my-story-beat-04-corplease-manager.mp4" poster="../assets/about-beat-04-corplease-manager.png" autoplay loop muted playsinline preload="auto"></video>' +
          '<div class="story-beat-caption"><span class="story-beat-caption-inner">' + captionLetters + '</span></div>' +
        '</div>';

      var vid = screen.querySelector('video.gs-intro-video');
      if (vid) { var pp = vid.play(); if (pp && pp.catch) pp.catch(function(){}); }

      var letters = screen.querySelectorAll('.story-beat-caption .cap-letter');
      if (hasGsap && letters.length) {
        gsap.set(letters, { opacity: 0, y: 4 });
        gsap.to(letters, {
          opacity: 1,
          y: 0,
          duration: 0.18,
          ease: 'power1.out',
          stagger: 0.05,
          delay: 0.5
        });
      } else if (letters.length) {
        for (var li = 0; li < letters.length; li++) {
          (function(el, idx){
            setTimeout(function(){ el.style.opacity = 1; }, 500 + idx * 50);
          })(letters[li], li);
        }
      }

      if (hasGsap) {
        gsap.fromTo(screen.querySelector('.gs-intro-fullscreen'),
          { opacity: 0 }, { opacity: 1, duration: 0.6, ease: 'power2.out' });
      }
    },
    function() {
      var captionText = "From 2020 to 2023 I served as Senior Vice President of Strategic Ventures at Banque Misr, Egypt's second largest national bank. I led sector analysis across fifteen plus industries with McKinsey and BCG as strategy partners, uncovered a hundred billion in export potential, and presented to cabinet ministers. Peak finance. And yet I knew I wanted more.";
      var captionLetters = '';
      var words = captionText.split(' ');
      for (var w = 0; w < words.length; w++) {
        captionLetters += '<span class="cap-word">';
        for (var c = 0; c < words[w].length; c++) {
          var ch = words[w].charAt(c);
          captionLetters += '<span class="cap-letter">' + ch.replace('<', '&lt;').replace('>', '&gt;') + '</span>';
        }
        captionLetters += '</span>';
        if (w < words.length - 1) {
          captionLetters += '<span class="cap-letter cap-space">&nbsp;</span>';
        }
      }
      screen.innerHTML =
        '<div class="gs-intro-fullscreen">' +
          '<video class="gs-intro-video" src="../assets/my-story-beat-05-banque-misr.mp4" poster="../assets/my-story-beat-05-banque-misr.png" autoplay loop muted playsinline preload="auto"></video>' +
          '<div class="story-beat-caption"><span class="story-beat-caption-inner">' + captionLetters + '</span></div>' +
        '</div>';

      var vid = screen.querySelector('video.gs-intro-video');
      if (vid) { var pp = vid.play(); if (pp && pp.catch) pp.catch(function(){}); }

      var letters = screen.querySelectorAll('.story-beat-caption .cap-letter');
      if (hasGsap && letters.length) {
        gsap.set(letters, { opacity: 0, y: 4 });
        gsap.to(letters, {
          opacity: 1,
          y: 0,
          duration: 0.18,
          ease: 'power1.out',
          stagger: 0.05,
          delay: 0.5
        });
      } else if (letters.length) {
        for (var li = 0; li < letters.length; li++) {
          (function(el, idx){
            setTimeout(function(){ el.style.opacity = 1; }, 500 + idx * 50);
          })(letters[li], li);
        }
      }

      if (hasGsap) {
        gsap.fromTo(screen.querySelector('.gs-intro-fullscreen'),
          { opacity: 0 }, { opacity: 1, duration: 0.6, ease: 'power2.out' });
      }
    },
    function() {
      screen.innerHTML =
        '<div class="gs-about-scene">' +
          '<div class="gs-about-image"><picture>' +
            '<source srcset="../assets/about-beat-06-ucsd.webp" type="image/webp">' +
            '<img src="../assets/about-beat-06-ucsd.png" alt="" loading="lazy" />' +
          '</picture></div>' +
          '<div class="gs-about-note">' +
            '<div class="gs-bubble" style="text-align:left !important;"><p>In 2025 I started the MSBA program at University of California San Diego, Rady School of Management. STEM designated. Business meets machine learning. I traded a Senior VP title for a student ID, and it was the best decision I have made.</p></div>' +
            '<div class="gs-about-note-secondary">GPA 3.95</div>' +
          '</div>' +
        '</div>';
      if (hasGsap) {
        gsap.fromTo(screen.querySelector('.gs-about-image'),
          { x: -20, opacity: 0 }, { x: 0, opacity: 1, duration: 0.4, ease: 'power2.out' });
        gsap.fromTo(screen.querySelector('.gs-bubble'),
          { opacity: 0 }, { opacity: 1, duration: 0.4, ease: 'power2.out', delay: 0.15 });
        gsap.fromTo(screen.querySelector('.gs-about-note-secondary'),
          { opacity: 0, y: 10 }, { opacity: 1, y: 0, duration: 0.3, ease: 'back.out(1.3)', delay: 0.4 });
      }
    },
    makeScene('../assets/about-beat-07-toolkit.png',
      'Today my toolkit is Python, SQL, Tableau, XGBoost, NLP, regression, classification, and everything in between. Every tool earned on a real project, from marketing campaign modeling to FIFA World Cup predictions to recommender systems.'),
    makeScene('../assets/about-beat-08-biking.png',
      'When I need to think, I ride. Road biking along the California coast. Miles of Pacific on one side, Torrey Pines on the other. Somewhere between mile ten and mile twenty, every problem unknots itself.'),
    makeScene('../assets/about-beat-09-barca.png',
      'I am forever blaugrana. Watching Barca play is watching teamwork and strategy at the highest level. Every match is a masterclass. Visca Barca.'),
    makeScene('../assets/about-beat-10-travel.png',
      'I have been lucky to see a lot of the world. Fifty plus cities across Europe, the Middle East, Asia, and North America. Hiking, camping, skiing, exploring. The best moments always happen off the tourist trail.'),
    makeScene('../assets/about-beat-11-sourdough.png',
      'I bake sourdough. It turns out fermentation, hydration math, and patient precision are the same skills I use in data analytics. The analyst bakes bread.'),
    makeScene('../assets/about-beat-12-future.png',
      'That is my story so far. Now let\'s talk about what comes next. Sales operations, marketing analytics, business intelligence, anywhere systems thinking meets quantitative rigor. I am open to opportunity and excited about what we might build together. Reach out anytime.')
  ];

  function goTo(n) {
    if (n < 0 || n >= scenes.length) return;
    currentScene = n;
    clearScreen();
    scenes[n]();
    tlFill.style.width = ((n + 1) / scenes.length * 100) + '%';
    counterEl.textContent = (n + 1) + ' / ' + scenes.length;
    prevBtn.disabled = (n === 0);
    nextBtn.disabled = (n === scenes.length - 1);
  }

  prevBtn.addEventListener('click', function() { goTo(currentScene - 1); });
  nextBtn.addEventListener('click', function() { goTo(currentScene + 1); });
  restartBtn.addEventListener('click', function() { goTo(0); });

  document.addEventListener('keydown', function(e) {
    var rect = document.getElementById('gs-theater').getBoundingClientRect();
    if (rect.top > window.innerHeight || rect.bottom < 0) return;
    if (e.key === 'ArrowRight') { goTo(currentScene + 1); e.preventDefault(); }
    if (e.key === 'ArrowLeft') { goTo(currentScene - 1); e.preventDefault(); }
  });

  goTo(0);
}
if (typeof gsap !== 'undefined') { initAboutTheater(); }
else { document.addEventListener('gsap-ready', initAboutTheater); }
</script>

<div style="text-align:center;padding:2rem 0 1rem;border-top:1px solid rgba(44,62,80,0.08);margin-top:1rem;">
  <div style="display:flex;gap:1rem;justify-content:center;flex-wrap:wrap;">
    <a href="mailto:msaad@ucsd.edu" class="about-link-btn about-link-filled" style="font-size:0.8rem;padding:6px 18px;">Email</a>
    <a href="https://www.linkedin.com/in/merna-s-saad" class="about-link-btn" style="font-size:0.8rem;padding:6px 18px;">LinkedIn</a>
    <a href="https://github.com/merna-saad" class="about-link-btn" style="font-size:0.8rem;padding:6px 18px;">GitHub</a>
  </div>
</div>
```
